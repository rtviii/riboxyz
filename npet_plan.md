Need to untangle the npet pipeline:

- simplify stages (could be one file)
- stages produce assets -- these should be well typed
- pipeline orchestrator provides interface to the stages
- visualizations library articulated in terms of artifacts
